(function (exports) {
    'use strict';

    const constants = {
        LEFT: 'left',
        RIGHT: 'right'
    };

    Object.freeze(constants);

    exports.nodeconstants = constants;
}(window));
